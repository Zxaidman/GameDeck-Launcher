<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Compose supplies the application's own theming. This platform theme exists only so the
      window has sensible defaults before the first frame is composed.
    -->
    <style name="Theme.Kestrel" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
